import { Shared2Directive } from './shared2.directive';

describe('Shared2Directive', () => {
  it('should create an instance', () => {
    const directive = new Shared2Directive();
    expect(directive).toBeTruthy();
  });
});
